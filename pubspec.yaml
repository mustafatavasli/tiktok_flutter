name: tiktok_flutter

description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  #FLUTTER_SDK
  flutter:
    sdk: flutter
  #UI
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.6
  #FIREBASE

  #DEPENDENCIES_INJECTION (DI)
  get_it: ^7.7.0
  #STATE_MANAGEMENT
  stacked: ^3.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg